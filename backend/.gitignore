# Environment variables
.env

# PEM files
keys/private.pem
*.pem
!keys/public.pem

# Keys directory (keep the directory but ignore private key)
# /keys/ is handled above by specific rules

# Optional: ignore all .env variants